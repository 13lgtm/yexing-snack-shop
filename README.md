# 业兴小吃 - 中式小吃订餐应用

一个现代化的中式小吃线上订餐系统，采用 React + TypeScript + Vite + Supabase 构建的全栈应用。

## ✨ 功能特点

- 📱 **精美UI设计** - 现代简约的移动端界面，Material Icons图标
- 🛒 **完整购物流程** - 浏览产品、加入购物车、在线结账
- 📦 **订单管理** - 实时订单状态、历史订单查询
- 🗄️ **Supabase后端** - 完整的数据持久化和API集成
- ⚡ **快速响应** - Vite构建工具，开发体验出色
- 🎨 **响应式设计** - 适配移动设备的流畅体验

## 🛠️ 技术栈

- **前端框架**: React 19 + TypeScript
- **构建工具**: Vite 6
- **路由**: React Router DOM 7
- **后端服务**: Supabase (PostgreSQL + REST API)
- **样式**: Tailwind CSS
- **图标**: Material Icons

## 🚀 快速开始

### 前提条件

- Node.js 18+ 
- npm 或 yarn
- Supabase 账号

### 安装步骤

1. **克隆或下载项目**
   ```bash
   cd 业兴小吃
   ```

2. **安装依赖**
   ```bash
   npm install
   ```

3. **配置Supabase数据库**
   
   详细的数据库设置步骤请查看 [SETUP.md](./SETUP.md)
   
   简要步骤：
   - 创建 Supabase 项目
   - 执行 `supabase_schema.sql` 创建表结构
   - 执行 `seed_data.sql` 导入初始数据

4. **配置环境变量**
   
   复制 `.env.example` 为 `.env.local`：
   ```bash
   cp .env.example .env.local
   ```
   
   然后编辑 `.env.local`，填入您的Supabase配置：
   ```env
   VITE_SUPABASE_URL=your_supabase_project_url
   VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
   ```

5. **运行开发服务器**
   ```bash
   npm run dev
   ```
   
   应用将在 `http://localhost:5173` 启动

6. **构建生产版本**
   ```bash
   npm run build
   ```

## 📁 项目结构

```
业兴小吃/
├── pages/              # 页面组件
│   ├── Home.tsx        # 首页 - 产品列表
│   ├── ProductDetail.tsx  # 产品详情页
│   ├── Cart.tsx        # 购物车
│   ├── Checkout.tsx    # 结账页面
│   ├── Receipt.tsx     # 订单收据
│   └── OrderHistory.tsx   # 订单历史
├── services/           # API服务
│   └── api.ts          # Supabase API调用
├── src/lib/            # 工具库
│   └── supabase.ts     # Supabase客户端配置
├── types.ts            # TypeScript类型定义
├── CartContext.tsx     # 购物车状态管理
├── constants.tsx       # 常量配置
├── supabase_schema.sql # 数据库架构
├── seed_data.sql       # 初始化数据
└── SETUP.md            # 详细配置指南
```

## 💾 数据库架构

应用使用4个主要数据表：

- **categories** - 产品分类（现做小吃、手作糖水、自制配料）
- **products** - 产品信息（包含价格、图片、评分等）
- **orders** - 订单主表
- **order_items** - 订单项目明细

详细的数据库设计请查看 `supabase_schema.sql`

## 🎯 主要功能

### 产品浏览
- 按分类筛选产品
- 查看产品详情（价格、评分、食材等）
- 实时库存显示

### 购物车
- 添加/删除产品
- 数量调整
- 实时价格计算

### 订单管理
- 选择配送方式（外送/自提）
- 添加订单备注
- 实时订单追踪
- 历史订单查看

## 🔧 开发指南

### 添加新产品

1. 在Supabase Table Editor中直接添加数据，或
2. 使用SQL插入：
   ```sql
   INSERT INTO products (id, name, price, description, category_id, image, rating, reviews_count, time_estimate, calories, ingredients)
   VALUES ('11', '新产品名', 12, '产品描述', '1', 'image_url', 4.5, '100+', '5分钟', '100千卡', '[{"emoji": "🥟", "name": "食材"}]'::jsonb);
   ```

### 自定义主题色

在 `index.html` 中修改 CSS 变量：
```css
:root {
  --primary: #dc2626;  /* 主色调 */
  --accent-red: #fef2f2; /* 强调色 */
}
```

## 📝 环境变量说明

| 变量名 | 说明 | 必需 |
|--------|------|------|
| `VITE_SUPABASE_URL` | Supabase 项目URL | ✅ |
| `VITE_SUPABASE_ANON_KEY` | Supabase 匿名密钥 | ✅ |

## 🐛 故障排除

**问题**: 页面显示"加载数据失败"
- 检查 `.env.local` 配置是否正确
- 确认 Supabase 项目正在运行
- 查看浏览器控制台错误信息

**问题**: 无法提交订单
- 检查数据库 RLS 策略是否正确设置
- 确认已执行完整的 `supabase_schema.sql`

更多问题请查看 [SETUP.md](./SETUP.md) 的常见问题章节。

## 📄 许可证

本项目为演示项目，仅供学习参考。

## 🙏 致谢

- 设计灵感来自现代食品外卖应用
- 使用 [Supabase](https://supabase.com) 提供后端服务
- UI 图标来自 [Material Icons](https://fonts.google.com/icons)
